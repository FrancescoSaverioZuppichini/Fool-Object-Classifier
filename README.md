# Fool-Object-Classifier

In this repo is contained all the resources I used to make my medium article "TITLE HERE".

In `core` there is the code, in `images` the images used and in `paper` the original Paper.
